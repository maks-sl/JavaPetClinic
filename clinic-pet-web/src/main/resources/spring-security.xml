<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <http auto-config="true" use-expressions="true">
        <csrf disabled="true"/>
        <!--<intercept-url pattern="/client/search" access="isAnonymous()" />-->
        <intercept-url pattern="/login" access="isAnonymous()" />
        <intercept-url pattern="/*" access="isAuthenticated()" />
        <intercept-url pattern="/views/client/create.jsp" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/client/create" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/client/delete" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/client/edit" access="hasAnyRole('ROLE_ADMIN, ROLE_MANAGER')" />
        <intercept-url pattern="/pet/**" access="hasAnyRole('ROLE_ADMIN, ROLE_MANAGER')" />

        <form-login
        login-page="/login"
        default-target-url=""
        authentication-failure-url="/login"/>

        <logout logout-url="/logout" logout-success-url="/" />

    </http>

    <authentication-manager>
        <authentication-provider ref="provider"/>
    </authentication-manager>

</beans:beans>